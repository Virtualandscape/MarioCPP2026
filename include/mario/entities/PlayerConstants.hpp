#pragma once

namespace mario {
    namespace player {
        // Player size in pixels (width x height)
        constexpr float Width = 16.0f;
        constexpr float Height = 24.0f;
    } // namespace player
} // namespace mario
