#pragma once

namespace mario {
    namespace player {
        // Taille du joueur en pixels (largeur x hauteur)
        constexpr float Width = 16.0f;
        constexpr float Height = 24.0f;
    } // namespace player
} // namespace mario
